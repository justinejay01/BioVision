using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class LabelScript : MonoBehaviour
{
    //public TextMeshProUGUI lbl;

    // Start is called before the first frame update
    void Start()
    {
        //lbl = Instantiate(lbl, FindObjectOfType<Canvas>().transform).GetComponent<TextMeshProUGUI>();

    }

    // Update is called once per frame
    void Update()
    {
        //lbl.transform.position = Camera.main.WorldToScreenPoint(transform.position + new Vector3(0, 0, 0.5f, 0));

    }
}
